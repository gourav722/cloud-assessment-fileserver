apiVersion: v1
kind: Service

metadata:
  name: {{ include "fileserver.fullname" . }}

spec:
  type: ClusterIP

  selector:
    app: fileserver

  ports:
    - protocol: TCP

      port: {{ .Values.service.port }}

      targetPort: {{ .Values.service.port }}